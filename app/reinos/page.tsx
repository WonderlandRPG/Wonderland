import { PlayerNav } from "@/components/player-nav";
import { WorldMap } from "@/components/world/world-map";
import { realmLore } from "@/lib/game/world-lore";
export const metadata={title:"Reinos de Wonderland"};
export default function RealmsPage(){return <main className="lore-page"><PlayerNav/><div className="page-container world-lore-shell"><header className="world-lore-hero"><span className="eyebrow">Atlas dos cinco reinos</span><h1>Reinos de Wonderland</h1><p>Explore o mapa e conheça os povos, conflitos, climas e segredos de cada território.</p></header><WorldMap linkToRealms={false}/><section className="realm-dossiers">{realmLore.map((realm,index)=><article id={realm.key} key={realm.key} style={{"--realm":realm.color} as React.CSSProperties}><header><span>{String(index+1).padStart(2,"0")}</span><div><small>{realm.title}</small><h2>{realm.name}</h2><p>{realm.summary}</p></div><dl><div><dt>Clima</dt><dd>{realm.climate}</dd></div><div><dt>Povos</dt><dd>{realm.people}</dd></div></dl></header><div className="realm-dossiers__body"><section><small>HISTÓRIA</small><p>{realm.history}</p></section><aside><small>CURIOSIDADES</small>{realm.curiosities.map(item=><p key={item}>◆ {item}</p>)}</aside></div></article>)}</section></div></main>}
